import React from 'react'
import s from './Error.scss'

const Error = () => (
  <section className={s.container}>
    <span className={s.text}>{'sorry, there was an error :('}</span>
  </section>
)

export default Error
